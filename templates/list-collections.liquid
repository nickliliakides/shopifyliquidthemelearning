<h1>Collections</h1>

{% for collection in collections %}
  <div>
    <a href="{{ collection.url }}">
      <h2>{{ collection.title }}</h2>
      <p>No of products: {{ collection.all_products_count }}</p>
    </a>
  </div>
{% endfor %}
